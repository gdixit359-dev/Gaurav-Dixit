<div class="banner-image-text-wrapper">
  <div class="banner-image-inner">
    {%- if section.settings.background_image != blank -%}
      {% render 'image', image: section.settings.background_image, class: 'background-image mobile-hide' %}
    {%- endif -%}
    {%- if section.settings.background_image_mobile != blank -%}
      {% render 'image', image: section.settings.background_image_mobile, class: 'background-image desktop-hide' %}
    {%- endif -%}
    <div class="banner-image-text-content">
      {% if section.settings.heading != blank %}
        <h1>{{ section.settings.heading }}</h1>
      {%- endif -%}
      {%- if section.settings.description != blank %}
        <p>{{ section.settings.description }}</p>
      {%- endif -%}
      {%- if section.settings.button_text and section.settings.button_url %}
        <a href="{{ section.settings.button_url }}" class="button-primary button-dark">{{ section.settings.button_text }}
          {%- render 'cta-arrow' -%}
        </a>
      {%- endif -%}
    </div>
  </div>
    {%- if section.settings.bottom_text != blank -%}
      <div class="banner-image-bottom-text">
        <div class="page-width">
        <p>{{ section.settings.bottom_text }}</p>
        </div>
      </div>
    {%- endif -%}
</div>
{% schema %}
  {
    "name": "Banner Image with Text",
    "class": "banner-image-with-text",
    "settings": [
      {
        "type": "image_picker",
        "id": "background_image",
        "label": "Background Image"
      },
      {
        "type": "image_picker",
        "id": "background_image_mobile",
        "label": "Background Image Mobile"
      },
      {
        "type": "text",
        "id": "heading",
        "label": "Heading"
      },
      {
        "type": "textarea",
        "id": "description",
        "label": "Description"
      },
      {
        "type": "text",
        "id": "button_text",
        "label": "Button Text"
      },
      {
        "type": "url",
        "id": "button_url",
        "label": "Button URL"
      },
      {
        "type": "text",
        "id": "bottom_text",
        "label": "Bottom text"
      }
    ],
    "presets": [
      {
        "name": "Banner Image with Text",
        "category": "Custom Sections"
      }
    ]
  }
  {% endschema %}